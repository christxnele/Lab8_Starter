# Lab8-Starter

## Graceful Degradation & Service Workers
- Service workers are an example of graceful degradation because Service Workers allow offline capabilities if they are supported. If a users' browser doesnt support service workers, the app will go back to normal network requests. By implementing service workers first, we are working from the top down, which is the process of graceful degradation.